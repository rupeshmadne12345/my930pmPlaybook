--- #my tasks playbook
- hosts: webservers 
  user: ansible
  become: yes
  connection: ssh

  tasks:
    - name: Installing httpd s/w in webservers
      action: yum name=httpd state=installed
      ignore_errors: yes

